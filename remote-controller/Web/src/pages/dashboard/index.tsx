import { Typography } from '@mui/material';
import PageTitle from 'layout/utils/page-title';

const Dashboard = () => (
    <PageTitle title="Welcome">
        <Typography>
            Welcome to the Smart Cd Player Controller configuration utility. Use
            the navigation menu to access different settings and features of the
            application.
        </Typography>
    </PageTitle>
);

export default Dashboard;
