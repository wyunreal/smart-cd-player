import { StepProps } from "@/app/components/client/flow";
import { AddCdToPlayerData } from "./types";

const Confirm = ({ data }: StepProps<AddCdToPlayerData>) => (
  <>{"Confirmation step"}</>
);

export default Confirm;
